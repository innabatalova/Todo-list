import { FC, ReactElement } from 'react'

import styles from './App.module.scss'

const VisibilityFilters: FC = (): ReactElement => {
  return (
    <div className={styles.VisibilityFilters}>
      VisibilityFilters
    </div>
  );
}

export default VisibilityFilters